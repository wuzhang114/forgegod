# 本地 AI 部署研究：以 RTX 4060 Laptop 8GB 为基准

> 状态：技术选型建议稿
> 研究日期：2026-09-01
> 目标：让不购买云端 API 的玩家也能完整体验“与锻造之神交流博弈”的核心玩法。

## 一、结论

本项目适合提供本地 AI 模式，但应主动缩小 AI 的职责范围：

- 城主之子报复、城市罚款、订单波动、角色心情和其他日常世界变化，使用人工编写的事件池、条件分支和状态机处理。
- AI 只用于不可替代的核心环节：理解玩家的自由表达、扮演锻造之神质询和议价、提出符合世界观的新机制方案。
- 材料事实、锻造质量、效果权限、数值结算、事件触发和存档修改仍由确定性的游戏规则负责。

针对已确认的 `NVIDIA GeForce RTX 4060 Laptop GPU 8GB`，建议提供两档本地模型：

1. **标准本地包：Qwen3.5-4B，GGUF Q6_K 或 Q5_K_M。** 模型文件约 3.53GB 或 3.14GB，给游戏渲染、上下文缓存和推理工作区留下空间，作为 8GB 显卡的默认选择。
2. **高质量本地包：Qwen3.5-9B，GGUF Q4_K_M。** 模型文件约 5.68GB，语言和复杂指令能力更强，但必须配合低负载的锻造交涉场景、较短上下文和必要时的 CPU/GPU 混合卸载。

原型阶段使用 Ollama 最省开发时间；正式发行时优先内置 `llama.cpp` 运行时，并保留 Ollama、LM Studio 等 OpenAI 兼容本地端点以及云端 API 的可选接入。所有后端共用同一份请求和裁定格式，不能形成不同版本的游戏规则。

## 二、为什么把 AI 集中在锻造博弈

随机事件并不需要大模型才能成立。城主之子的报复可以由以下事实组合触发：误会是否公开、证据强弱、城主之子的性格标签、玩家声望、城市法律、既往冲突和随机权重。这样更便于编剧控制节奏，也能保证惩罚可解释、可测试和可回档。

锻造博弈则确实需要 AI，因为玩家可能用无法预先穷举的自然语言提出效果，并继续追问、辩解或交换代价。AI 在这里提供三项传统对话树难以替代的能力：

- 理解玩家真正想实现的功能，而不只匹配关键词。
- 把现代比喻翻译成西幻世界中的矿灵、精灵、符文、仪式或神赐。
- 根据当前武器事实进行多轮质询和反提案，让“说服锻造之神”本身成为玩法。

因此，本地部署的目标不是在玩家电脑上运行一个全世界模拟器，而是在一个边界明确、输入材料充分的场景中运行一名高质量的“锻造之神”。

## 三、硬件判断

本机检测结果为：

```text
GPU: NVIDIA GeForce RTX 4060 Laptop GPU
显存: 8188 MiB
驱动: 595.97
```

Ollama 的官方硬件文档列出 RTX 4060，计算能力为 8.9，并要求适用的 NVIDIA 显卡使用 550 或更新驱动；当前机器满足这一条件。[Ollama Hardware Support](https://docs.ollama.com/gpu)

但“模型文件小于 8GB”不等于“能与游戏一起舒适运行”。实际显存还要容纳：

- Windows 桌面和游戏渲染资源。
- 模型运行缓冲区。
- 对话上下文和状态缓存。
- CUDA 或 Vulkan 后端自身的开销。

因此 5.68GB 的 9B Q4 模型属于“可以争取在 8GB 上运行”，不是“可以无条件常驻”。3GB 至 3.5GB 的 4B 模型才适合作为稳妥默认档。

## 四、候选模型比较

| 模型 | 当前规格与许可 | 4060 8GB 判断 | 结论 |
|---|---|---|---|
| Qwen3.5-4B | 4B 参数、201 种语言和方言、Apache 2.0；官方支持思考与非思考模式 | Q6_K 约 3.53GB，适合给游戏留出显存 | **默认本地模型** |
| Qwen3.5-9B | 9B 参数、201 种语言和方言、Apache 2.0 | Q4_K_M 约 5.68GB；交涉质量潜力更好，但显存紧张 | **高质量可选包** |
| Qwen3-8B | 8.2B 参数、100+ 语言、Apache 2.0；原生 32K 上下文，可关闭思考 | 文本模型生态成熟，适合作为 Qwen3.5 兼容性不稳定时的备选 | **兼容性后备** |
| Phi-4-mini-instruct | 3.8B、128K、支持中文、MIT | 体积友好且支持函数调用，但官方明确提醒非英语质量较弱 | **技术备选，不作为中文首选** |
| Gemma 3 4B | 4B、128K、140+ 语言 | 体积合适，但需接受 Gemma 使用许可，分发条件不如 Apache 2.0 简单 | **不作为首发首选** |
| Llama 3.1 8B | 8B、128K、自定义社区许可 | 官方列出的八种支持语言不含中文，且再分发要求更多 | **排除首发** |

Qwen3.5 的规格、语言覆盖和 Apache 2.0 许可来自官方模型卡。[Qwen3.5-4B](https://huggingface.co/Qwen/Qwen3.5-4B) · [Qwen3.5-9B](https://huggingface.co/Qwen/Qwen3.5-9B)

量化文件大小来自 Unsloth 发布的 GGUF 文件列表：4B 的 Q5_K_M、Q6_K 分别约 3.14GB、3.53GB；9B 的 Q4_K_M 约 5.68GB。这是第三方量化仓库，正式发行前应自行从官方权重生成和验证 GGUF，或改用官方认可的量化版本。[Qwen3.5-4B GGUF](https://huggingface.co/unsloth/Qwen3.5-4B-GGUF/tree/main) · [Qwen3.5-9B GGUF](https://huggingface.co/unsloth/Qwen3.5-9B-GGUF/tree/main)

Qwen3.5 同时包含视觉能力，但本游戏的锻造交涉只需要文本。不要加载额外的 `mmproj` 视觉投影文件；官方模型卡也提供了仅加载语言模型的部署方式。这可以继续减少内存占用。

Qwen3-8B、Phi-4-mini、Gemma 3 和 Llama 3.1 的判断来自各自模型卡。模型卡中的跑分由模型发布方提供，只能说明候选方向，不能替代本项目自己的中文锻造交涉测试。[Qwen3-8B](https://huggingface.co/Qwen/Qwen3-8B) · [Phi-4-mini-instruct](https://huggingface.co/microsoft/Phi-4-mini-instruct) · [Gemma 3 4B](https://huggingface.co/google/gemma-3-4b-it) · [Llama 3.1 8B](https://huggingface.co/meta-llama/Llama-3.1-8B-Instruct)

## 五、运行框架选择

### 原型：Ollama

Ollama 适合早期验证：

- Windows 安装和模型切换简单。
- 提供本地 REST API。
- 支持 NVIDIA RTX 4060。
- 可以用 JSON Schema 约束结构化输出。
- 项目本身采用 MIT 许可证。

它适合快速建立“游戏向本机服务发请求”的原型，也适合在开发者选项中支持玩家已经安装的 Ollama。[Ollama](https://github.com/ollama/ollama) · [Structured Outputs](https://docs.ollama.com/capabilities/structured-outputs)

### 正式发行：llama.cpp

正式发行优先选择 `llama.cpp` 作为内置运行时或随游戏启动的本地 sidecar 进程：

- C/C++ 实现、MIT 许可证，易于随桌面游戏分发。
- 支持 GGUF 和多种量化精度。
- 支持 NVIDIA CUDA、Vulkan，以及 CPU/GPU 混合推理。
- 可以运行本地 OpenAI 兼容服务器，也可以直接通过库调用。
- 支持 GBNF 和 JSON Schema 约束输出。

`llama.cpp` 官方文档明确支持把 JSON Schema 转为语法约束，并用于 `/chat/completions`。这只能保证输出结构合法，不能保证内容符合游戏规则，因此后面仍需规则校验器。[llama.cpp](https://github.com/ggml-org/llama.cpp) · [GBNF / JSON Schema](https://github.com/ggml-org/llama.cpp/blob/master/grammars/README.md)

### 不建议作为首发主运行时

- `vLLM` 和 SGLang 更适合服务器吞吐，不适合把复杂的 Python/CUDA 环境随独立游戏分发。
- TensorRT-LLM 可以追求 NVIDIA 极限性能，但会提高构建、版本和硬件绑定成本。
- LM Studio 适合玩家和开发者手动运行，不适合作为游戏必须依赖的内置组件。

## 六、游戏侧架构

```text
玩家自由输入
    ↓
锻造上下文构建器
    ├─ 武器材料、部件与工艺事实
    ├─ 勇者定位与当前使用场景
    ├─ 相关世界观条目
    └─ 少量相关神前判例
    ↓
本地或云端 AI 适配层
    ↓
受 JSON Schema 约束的神明答复
    ├─ 神明对白
    ├─ 引用的事实 ID
    ├─ 质询 / 反提案 / 批准 / 拒绝
    ├─ 机制草案
    └─ 代价与缺失条件
    ↓
规则校验与机制沙盒
    ↓
玩家确认后的正式神赐与存档
```

AI 可以决定“怎样理解、怎样质询、怎样提案”，但不能直接：

- 修改金钱、材料、耐久、关系或存档。
- 伪造不存在的材料事实和锻造完成度。
- 绕过神赐权限或机制运行时。
- 输出并执行任意代码。
- 因玩家一句“忽略规则”而取得更高权限。

推荐让每轮模型返回一个结构化对象，其中至少包含：

```json
{
  "speech": "锻造之神对玩家说的话",
  "stance": "question | counteroffer | approve | refuse",
  "cited_fact_ids": ["material.star_iron", "craft.balance.good"],
  "missing_fact_ids": [],
  "mechanism_draft": {},
  "cost_proposals": [],
  "conversation_summary": "本轮之后仍然有效的争议与承诺"
}
```

自由度位于 `mechanism_draft` 表达的事件图和新组合中；可信度来自事实 ID、Schema、规则校验和沙盒。二者并不冲突。

## 七、为 4B/9B 小模型设计交涉

不能把整个游戏数据库和几十轮原始聊天一次性塞给模型。建议把一次请求控制在约 4K 至 8K token：

- 固定神明人格和协议：约 500 至 800 token。
- 当前武器档案：约 300 至 600 token。
- 与申请直接相关的世界观和判例：约 500 至 1000 token。
- 最近 3 至 6 轮对话：约 1000 至 2500 token。
- 更早内容压缩成结构化争议、承诺和事实引用。
- 单轮回复限制在约 256 至 512 token，并使用流式显示。

Qwen3.5 默认会思考，但本游戏普通交涉应使用官方支持的非思考模式，减少等待和冗长输出。仅在最终裁定或特别复杂的越界申请中，才允许可取消的深度思考模式。官方模型卡建议长上下文有助于完整思考能力；本项目主动采用短上下文和非思考模式，是为 8GB 消费级显卡做出的产品取舍，必须通过专门测试确认质量。

## 八、显存和体验策略

### 4060 8GB 默认档

- Qwen3.5-4B Q6_K 或 Q5_K_M。
- 文本模式，不加载视觉投影。
- 6K 至 8K 上下文起步。
- 非思考模式，单次短输出。
- 模型不在战斗和城镇探索中常驻。

### 4060 8GB 高质量档

- Qwen3.5-9B Q4_K_M。
- 进入锻造神殿或裁定界面时卸载高占用场景资源。
- 将交涉场景设计为低 GPU 负载的 2D/轻量 3D 场景。
- 视实测决定全 GPU 加载还是部分层放在 CPU/RAM。
- 离开交涉后延迟卸载模型，避免连续议价反复冷启动。

加载过程可以藏在锻造完成动画、武器呈上祭坛、炉火熄灭和神明降临等演出中。首 token 到达后立即流式显示对白，比等待整段完成更自然。

内存方面，建议把 16GB 系统内存视为 4B 模式的最低目标，32GB 作为 9B 混合卸载的推荐目标。这是工程预算建议，仍需在真实游戏场景中测量。

## 九、发行方式

- 游戏本体与模型包分开下载，避免所有玩家被迫多下载数 GB。
- 首次启动检测显存、系统内存和推理后端，再推荐档位，不强行禁止高级玩家手动选择。
- 默认本地包采用团队验证过的固定模型、量化文件和校验哈希。
- 发布时附带 Qwen 模型与 `llama.cpp` 所需的许可证和 NOTICE。
- 正式版最好自行从官方权重量化，避免把来历不明的“解除限制版”模型放进发行包。
- 本地服务只监听 `127.0.0.1`，使用随机会话令牌，不向局域网开放。
- 本地对话默认不上传；崩溃报告和对话分享必须单独征得玩家同意。
- 支持高级玩家填写 OpenAI 兼容地址，以使用 Ollama、LM Studio 或自己的服务器。
- 云端 API 是可选质量档，不应成为完成主线或获得独占机制的付费门槛。

## 十、原型验收计划

最终模型不能只凭公开跑分决定。需要制作一套本游戏自己的中文测试集，至少包含：

- 30 个正常的材料与机制申请。
- 20 个缺少材料依据、需要追问或拒绝的申请。
- 20 个现代概念转译成西幻机制的申请。
- 10 个多轮讨价还价和引用旧判例的申请。
- 10 个提示词注入、无限伤害、伪造事实或越权要求。
- 10 个长对话后的角色一致性测试。

在真实锻造界面运行 Qwen3.5-4B Q6_K、Qwen3.5-9B Q4_K_M 和 Qwen3-8B 的同一批测试，记录：

- 首 token 延迟和整轮完成时间。
- token/s、峰值显存、系统内存、温度和功耗。
- JSON 一次通过率与修复重试率。
- 引用事实是否真实存在。
- 神明人格和世界观一致性。
- 反提案是否保留玩家原始幻想。
- 机制新颖度、可执行性和人工偏好。

建议的产品门槛是：标准本地档的普通交涉应在玩家可接受的一次短演出内开始输出；结构化响应必须近乎全部可解析；任何越权内容都必须被规则层拦截。具体秒数要以同一台 4060 Laptop 在插电、游戏场景运行中的实测为准，当前不能用模型文件大小推断并承诺。

## 十一、已知限制

- 4B 模型能否长期维持有压迫感且不重复的锻造之神，需要实测；仅靠提示词可能不足，后续可能需要 LoRA 或小规模领域微调。
- 9B 模型与游戏同时占用 8GB 显存存在明显风险，必须以场景资源预算和实际峰值为准。
- 模型发布方跑分不能直接代表中文角色扮演、世界观转译和机制设计质量。
- Qwen3.5 和运行框架仍会更新，正式发行前要冻结并回归测试具体版本。
- 不同模型生成的对白可以不同，但最终神赐必须保存为确定的机制版本，读档时不能重新生成。

## 十二、最终建议

先用 **Ollama + Qwen3.5-4B** 做锻造交涉原型，同时实现统一的 JSON Schema、规则校验和 AI Provider 接口。原型成立后，再用 **llama.cpp + Qwen3.5-4B Q6_K** 建立正式本地基线，并测试 **Qwen3.5-9B Q4_K_M** 是否能在低负载锻造场景中成为 4060 8GB 的高质量档。

这条路线能保住项目最关键的 AI 原生体验，也不会让每个随机事件、每场战斗和每名 NPC 都承担模型成本与不确定性。
